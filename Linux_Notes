/bin -> usr/bin
/boot -> boot loader programme for starting a linux machine
/dev   ---> this for device information like divice manager in windows
/etc    ->  all the system files and third party configuration files are present here.
/home  --> home directories for all users in linux
/initrd.img -> boot/initrd.img-4.18.0-kali2-amd64
/initrd.img.old -> boot/initrd.img-4.18.0-kali2-amd64
/lib -> usr/lib  --> this are librarys required for linux command fuctions
/lib32 -> usr/lib32 --> 
/lib64 -> usr/lib64
/libx32 -> usr/libx32
/lost+found
/media --> this is likea initial mount point  media related devices
/mnt --> mount point directory 
/opt --> optional file system , default d drive 
/proc --> all the info relating to processors is stored here, \/proc/cpuinfo , /proc/meminfo 
/root --> is home dir for root user
/run
/sbin -> usr/sbin thirp party files stored here 
/srv
/sys
/tmp --> is file system read write access for all users in linux 
/usr --> user file system 
/var --> file system for storing sytem logs and system data
/vmlinuz -> boot/vmlinuz-4.18.0-kali2-amd64
/vmlinuz.old -> boot/vmlinuz-4.18.0-kali2-amd64

----------------------

Debian - Debian/ubuntu/Kali/Raspberry   -->apt is app store. 
rhel(enterprise)/centos/oel(ent)  --> yum 
fedora  --> dnf 


epel repo --> this is a geneirc repo for rhel/centos/oel 

Every APp store is a configurataion file. --> /etc 

you can create a local yum. 

**Debian COmmands 
apt upgrade or apt-get upgrade 
apt full-upgrade or apt-get dist-upgrade
apt install <pkg-name> 
apt -y install <pkg-name>
apt remove <pkg-name>
apt-cache search <pkg-name> - give the pkg and description
apt-cache showpkg <pkg-name> - list dependencies 
apt-cache show <pkg-name> - show pkg info. 
apt-get clean (clean the .deb files downloaded)
apt-get download <pkg-name> - download witout install


**RHEL 

yum install <pkg-name>
yum -y install <pkg-name>
yum -y groupinstall <group-name>
yum search <pkg-name>
yum -v repolist 
yum remove <pkg-name>
yum reinstall <pkg-name>
yum info <pkg-name>
yum clean all  --> Clean Yum Cache
yum -y localinstall * --> install rpm files as yum. 



Note:- 
1. Every COnnection has a Port Number 
    <1024 --> owned by root , [sudo , stickybit has to be used if it has to start with anathor user.]
    >1024 - 9999 --> statuc Ports 
    9999 - 65535 --> dynamic ports. 

  important protocols and port numbers in linux. --> https://hostingreviewbox.com/rhel-tcp-and-udp-ports/

  ssh / ftp 

**Linux COmmands. 

1. OS Relatesd 

  cat /etc/*release        --> Linux Version 
  uname -a                 --> Kernel , flavour 32/64 bit. 
  cat /proc/cpuinfo        --> Processor(s) info  
  cat /proc/meminfo        --> Memory info. 
  free -m/-g/-k            --> total and free memory in mbs, gbs, kbs 
  top                      --> this is task manager in windows , system state 
  
          top - 22:17:23 up 51 min,  2 users,  load average: 0.00, 0.00, 0.00
          Tasks: 168 total,   1 running, 167 sleeping,   0 stopped,   0 zombie
          %Cpu(s):  0.0 us,  0.3 sy,  0.0 ni, 99.7 id,  0.0 wa,  0.0 hi,  0.0 si,  0.0 st
          MiB Mem :   1997.2 total,     95.2 free,    838.8 used,   1063.1 buff/cache
          MiB Swap:   7629.0 total,   7628.7 free,      0.3 used.    973.9 avail Mem
   a. system uptime 
   b. how many users are logged in (count) 
   c. Load avg = random number generated by linux x,y,z 
        x= current load avh 
		y= load avg 5 mins ago 
		z= load avg 10 mins ago 
   d. total processes/sleeping/zombie/running. 
   e. cpu ideal usage stats. 
   f. memory - sysrtem memory 
   g. swap memory 
   
   w or who                --> who are logged in to the system. 
   whoami or "who am i"    --> which user is me , it will give the info. 
                        whoami --> current user 
						who am i --> the parent user used to connect to the current user.    
  "date" --> system date
  "cal" --> Current MOnth calender 
  "cal -3"  --> last 3 months calender 
  "ls -l /etc/localtime" --> which timezone file it is pointing to 
  [osboxes@osboxes zoneinfo]$ ls -l /etc/localtime
  lrwxrwxrwx. 1 root root 38 Feb 11  2019 /etc/localtime -> ../usr/share/zoneinfo/America/New_York
  
  If you want to update the timezone to a different zone then you must recfreate the softlink to a new file. 
  
  [root@osboxes etc]# unlink localtime
  [root@osboxes etc]# ln -sf /usr/share/zoneinfo/Singapore localtime
  [root@osboxes etc]# ls -l /etc/localtime
  lrwxrwxrwx. 1 root root 29 Aug 25 12:25 /etc/localtime -> /usr/share/zoneinfo/Singapore

  man <command> 

2. users and groups.
 /etc/passwd --> users
 /etc/group --> groups info 
 /etc/shadow --> hashed passwords for user security. 
 /etc/gshadow --> to secure your groups. 
 useradd  --> useradd <username>  --> add users to linusx and it will also create a group as well. 
 usermod   --> usermod <switches> <user> --> modify the user. 
 groupadd --> groupadd <group>
 userdel --> delete the user --> userdel <user>
 groupdel --> delete the groups. --> groupdel <group_name> 
 id --> current user info 
 id <username> --> the userinfo of the targeted user.
 passwd --> interactive script to chanvge the password for current user. 
 passwd username ==> mostly used with root user and to change the password for other user.  
 
						
3. File / Directory Related 

 objects in linux 
     file 
	 folder 
	 hideen file 
	 hidden folder 
	 shortcut / softlink 

**file --> [root@osboxes files]# touch a  ---> -rw-r--r--. 1 root root    0 Aug 25 13:16 a     --> 0644 
**hiddenfile --> [root@osboxes files]# touch .a --> -rw-r--r--. 1 root root    0 Aug 25 13:16 .a --> 0644
**diredtory --> [root@osboxes files]# mkdir test --> drwxr-xr-x. 2 root root 4096 Aug 25 13:16 test --> 0755
**Hidden directory --> [root@osboxes files]# mkdir .test --> drwxr-xr-x. 2 root root 4096 Aug 25 13:16 .test --> 0755
**Shortcut or a softlink --> [root@osboxes files]# ln -s /tmp temp_syed --> lrwxrwxrwx. 1 root root    4 Aug 25 13:17 temp_syed -> /tmp --> 0777

-rw-r--r--  --> 10 digit representation (0-9) 
0 digit      --> file/directory/softlink (file - , folder d, link l)  
1,2,3        --> user permissions. 
4,5,6        --> group permissions. 
7,8,9        --> other permissions. 

r --> read --> r=4 
w --> write --> w=2 
x --> execute --> x=1 

umask  --> decider for new permissions of files, 
           umask for files = 0666 - umask value = 0666 - 0022 = 0644
		   umask for directories = 0777 - umask = 0777 - 0022 = 0755 
		   umask for links --> 0777 
chmod --> change the permssions of files.
          chmod <perm> filename 
		  chmod -R <perm> foldername --> update the permissions for the files and subdirectoires in folder. 

-rw-rw-rw-. 1 root root    0 Aug 25 13:34 abcdef
[root@osboxes files]# chmod 444 abcdef
[root@osboxes files]# ls -l
total 8
-r--r--r--. 1 root root    0 Aug 25 13:34 abcdef
   
chown --> CHange the ownership. 
          chown <user>:<group> file 
		  chown -R <user>:<group> folder (Recursively change permissions for users and grops subfolders) 


[root@osboxes files]# chown user1:dba abcdef
[root@osboxes files]# ls -l
total 8
-rw-r--r--. 1 root  root    0 Aug 25 13:16 a
-r--r--r--. 1 user1 dba     0 Aug 25 13:34 abcdef


	 
	 
 permissions on files 
 change the permissions. 
 
 ls  --> list the files in paragraph mode.  
    -l --> log listing (createion dagte, modified date, size) 
	-a --> list hidden files. 
	-h --> human readable 
	-r --> sort based on creation date. 
	-t --> timestamp
	-latrh --> long listing of fiLES in ascending order and it will show by creation date.  

 tree --> show the files and directories for you. 

 echo <text> -> print the text, variable./ creating the files 
  
  [root@osboxes etc]# echo hello
  hello
  [root@osboxes etc]# x=5 && echo value is x && echo actuval value $x
  value is x
  actuval value 5
  [root@osboxes etc]# echo hello > test.txt
  [root@osboxes etc]# cat test.txt
  hello
  [root@osboxes etc]#

  
 
 

vi editor 
file transfers
archiving files. 
sudoers. 

   > --> override. 
   >> --> append. 
   && --> execute both commands. 
   | --> 

4. ports process 

5. misc 
   sed
   awk 
   cron job 
   

6. Regular expresssion and learning searching files and patterns. 

7  Loops 
   


           
 encryption --> encrypt the value with algorith + key , if you want to decrypt then you need the alogorith and also the key for it. 
 encoding --> encrypting the value using some algorithms, this is symetric, same algorithm is used for encryption and decryption 
 hashing  --> encytpting the text but there is no reverse algorithm. 
 


  
